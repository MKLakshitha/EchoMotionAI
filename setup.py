from setuptools import setup

description = ['Make path configuration easier']

setup(
    name='EchoMotionAI',
    version='1.0',
    python_requires='>=3.9',
    author='Kavindu Lakshitha',
    description=description,
    packages=['lib']
)
